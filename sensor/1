import React from 'react';
import { Text, View, StyleSheet } from  'react-native';

export default class light extends React.Component {
	redner() {
		return (
			<View style={styles.container}>
			<Text style={styles.paragraph}>
			Lighting Control!!
			</Text>
			<Text style={styles.paragraph} source={require('../test/light.txt')} />
			</View>
		);
	}
}

export default class test extends React.Component {
	render() {
		return (
			<View style={styles.container}>
			<Text style={styles.paragraph}>
			Shopping Cart!
			</Text>

			<Text style={styles.paragraph} source={require('../test/jarfile.txt')} />
			</View>
		);
	}
}


const styles = StyleSheet.create( {
	paragraph: {
		margin: 14,
		fontSize: 18,
		fontWeight: 'bold',
		textAlign: 'center',
	},

	container: {
		flex:1,
		alignContent: 'center',
		padding: 8,
	},
});

